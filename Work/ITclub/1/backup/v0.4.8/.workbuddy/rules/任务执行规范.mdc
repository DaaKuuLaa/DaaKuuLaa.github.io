---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-15T14:31:05.518Z
provider: 
---

# 任务执行规范

## 任务目标
修复模块2（肯·柏林简介）第一页时间轴的显示问题

## 问题分析

### 根本原因
1. **圆点与时间轴不对齐**：CSS定位计算偏差，圆点中心点与时间轴线不重合
2. **数字被遮挡**：左侧空间不足（left: -56px），导致年份数字显示不完整
3. **颜色相同导致看不清**：圆点（background: var(--color-primary)）与数字（color: var(--color-primary)）使用相同颜色变量

### 具体位置
- **文件**：`a:\Work\css\components.css`
- **样式类**：`.event-item::before`（圆点）、`.event-year`（年份数字）
- **行号**：835-859行

## 行动方案

### 阶段1：问题诊断（已完成）
- ✅ 定位到问题代码位置
- ✅ 分析CSS样式冲突
- ✅ 识别颜色变量使用问题

### 阶段2：方案设计
1. **调整时间轴线条位置**：优化`left`和`width`值，确保圆点能对准中心
2. **增加左侧间距**：扩大`padding-left`和`left`值，给年份数字足够空间
3. **优化圆点定位**：调整`left`值，使其精确对准时间轴
4. **修改数字颜色**：使用浅蓝色（`var(--color-primary-light)`）
5. **添加文字阴影**：使用`text-shadow`添加淡淡的小阴影效果
6. **优化数字背景**：使用半透明白色背景，提高可读性

### 阶段3：实施步骤

#### 任务1：修复时间轴对齐问题
- **文件**：`css/components.css`
- **修改点**：
  - `.events-timeline-container::before`：调整`left: 24px`和`width: 3px`
  - `.event-item::before`：调整`left: -36px`确保圆点对准时间轴
  - `.event-year`：调整`left: -56px`避免遮挡

#### 任务2：修改数字颜色和添加阴影
- **文件**：`css/components.css`
- **修改点**：
  - `.event-year`的`color`：从`var(--color-primary)`改为`var(--color-primary-light)`
  - 添加`text-shadow: 0 1px 2px rgba(0,0,0,0.2)`
  - 优化背景色为半透明白色：`rgba(255,255,255,0.1)`

#### 任务3：验证和测试
- 在1920x1080分辨率下测试
- 在1366x768分辨率下测试
- 检查无垂直滚动条
- 验证毛玻璃效果正常

### 阶段4：日志记录
按照RULE.md 4.4章节要求，详细记录所有修改：
- 修改时间
- 修改类型
- 文件路径
- 修改位置
- 修改内容（旧值和新值）
- 修改原因
- 验证状态
- 验证结果

### 预期效果
1. 圆点精确对准时间轴中心线
2. 年份数字清晰可见，无遮挡
3. 数字颜色为浅蓝色，与圆点区分明显
4. 数字有淡淡阴影，增强可读性
5. 保持毛玻璃视觉效果
6. 符合项目色彩系统规范