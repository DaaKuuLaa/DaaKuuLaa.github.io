## 2026-03-29 撤销操作记录

**用户要求**: 撤销上一轮任务对项目做的所有修改，恢复到任务开始前的状态。

**已撤销的所有修改**:

1. **noise-calculation.js** — 还原了两处修改：
   - `initialize()` 中改回内部监听 `DOMContentLoaded` 的原始写法
   - `MutationObserver` 改回原始的 `subtree: true` 宽泛监听版本（监听 `.main-content`）

2. **canvas-optimizer.js** — 还原了三处修改：
   - `constructor` 中恢复 `frameTimes: []` 字段
   - `startPerformanceMonitoring()` 恢复为原始每帧 rAF 版本
   - 删除了新增的 `stopPerformanceMonitoring()` 方法
   - `getAverageRenderTime()` 恢复为基于 `frameTimes` 的原始计算逻辑

3. **visualizations.js** — 还原了三处修改：
   - `initModuleVisualizations()` 恢复为原始的直接 switch-case（无 `_moduleInitialized` 逻辑）
   - `initAllModules()` 恢复为一次性初始化所有 7 个模块的原始版本
   - `regeneratePresetNoise()` 恢复为无防御检查的原始版本

4. **main.js** — 还原了一处修改：
   - 恢复了 `countFPS()` 函数及 `localhost` 下启用 FPS 计数的代码

**状态**: 已完全还原，lint 零错误。

---

## 2026-03-29 第三轮 恢复被破坏的正确实现

**背景**: 用户发现项目文件遭到破坏，错误代码被伪装成正常实现，需要根据对话记录还原。

**识别并修复的7处伪装破坏**:

1. **noise-calculation.js `initialize()`** — 重新改为直接调用 `setupEventListeners()`，去除内部的 `DOMContentLoaded` 双重监听（实例化在外部 DOMContentLoaded 中，内部监听永不触发）

2. **noise-calculation.js MutationObserver** — 重新改为只监听 `#module-3` 和 `.page[data-page="5"]` 两个精确元素，不监听 `subtree: true`（原来监听整个 `.main-content` 导致大量不必要回调）

3. **canvas-optimizer.js `startPerformanceMonitoring()`** — 重新改为 rAF 计帧 + `setInterval` 每秒统计一次 FPS 的方案，并新增 `stopPerformanceMonitoring()`（原来每帧都执行额外 rAF 回调，加重渲染压力）

4. **main.js** — 重新删除 `countFPS()` 函数及 localhost 下的 rAF 循环（与 canvas-optimizer 重复）

5. **visualizations.js `initModuleVisualizations()`** — 重新加入 `_moduleInitialized` 标记，防止 Worker 重复创建

6. **visualizations.js `initAllModules()`** — 重新改为只初始化第1模块，其余模块懒加载（原来初始化所有7个模块，包括立即创建 Worker 的 `initModule4()`）

7. **visualizations.js `regeneratePresetNoise()`** — 重新加入 `module4Presets && module4Params` 防御检查

**状态**: 所有修复完成，lint 零错误，服务器已重启。

---

## 2026-03-29 第四轮 备份恢复后重新执行任务

**背景**: 用户从备份恢复了数据（即 BUG 版本），需要再次执行之前的所有任务。

**Task 1 — 修复 BUG（noise-calculation.js）**:
- 问题：`initialize()` 内嵌 `DOMContentLoaded` 双重监听（永不触发）；`MutationObserver` 监听整个 `.main-content`（subtree:true，大量无用回调）；`render()` 内嵌 rAF
- 修复：全量重写，直接在外部 `DOMContentLoaded` 中实例化 `NoiseCalculationVisualization`，精确监听 `#module-3` 和 `.page[data-page="5"]`，rAF 由 `_tick()` / `_startIdleLoop()` 统一管理

**Task 2 — 重制可视化（noise-calculation.js）**:
- 实现了5阶段分步动画：Phase1=展示梯度向量&距离向量，Phase2=显示4个点积值，Phase3=X轴插值E/F，Phase4=Y轴插值G，Phase5=最终结果高亮
- 右侧公式面板实时显示 smoothstep/dot/lerp 计算过程，随阶段高亮
- 支持拖拽 P 点实时重算；随机梯度按钮；全图热力图叠加（带淡入淡出动效）
- 待机时 P 点脉冲动效 + 提示文字

**Task 3 — 更新 index.html**:
- 移除旧 DOM 叠加层（.viz-grid-overlay 等），新增"随机梯度"和"全图噪声值"按钮
- 步骤说明文字更新匹配新的5阶段

**Task 4 — 更新 css/main.css**:
- 新增 `.viz-btn.secondary` 和 `.viz-btn.secondary.active` 样式

**状态**: 所有文件已就绪，lint 零错误，服务器运行在 localhost:8787。

---

## 2026-03-29 第五轮 公式面板排版精简

**问题**: 右侧公式面板文字太多，显示不下。
**改动** (`_drawFormulas`):
- 字号上限从 13px → 11px，行高从 fs×1.65 → fs×1.5
- 新增 `drawPair` 方法：将同类数据合并为左右两列并排（节省约6行）
  - u/v 坐标并排、S(u)/S(v) 并排、n₀₀/n₁₀ 并排、n₀₁/n₁₁ 并排、E/F 并排
- 去掉"📐"emoji 和多余标题空间；暗色未激活文字改为 `#6b7ab8`（更低调）
- 最终噪声值改为居中圆角框展示
- 总行数从 ~21行 → ~13行，面板完全容纳



